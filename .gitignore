logs/
*.jsonl*

user_data/

# runtime files (generated at runtime, never commit)
scripts/runtime/

# bookmarks is user data, but can be shared as template
bookmarks.json

screenshots/
downloads/

!.gitignore
